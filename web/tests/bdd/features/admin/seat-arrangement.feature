# language: zh-CN
@p1 @admin @seat-arrangement
功能: 座位安排
  作为租户管理员
  我想要安排考场座位
  以便组织考试

  背景:
    假如 系统已经启动并运行
    并且 存在租户管理员账号 "tenant_admin"
    并且 报名已截止
    并且 所有考生已完成缴费

  @smoke
  场景: 管理员配置考场
    假如 我使用用户名 "tenant_admin" 和密码 "TenantAdmin123!@#" 登录
    并且 我选择租户 "test-company-a"
    当 我访问座位安排页面
    并且 我选择考试 "2025年春季招聘考试"
    并且 我点击"配置考场"按钮
    并且 我填写考场信息
      | 考场名称   | 第一考场                |
      | 考场地址   | 北京市海淀区中关村大街1号 |
      | 教室数量   | 5                       |
    并且 我点击保存按钮
    那么 考场配置应该成功
    并且 我应该看到成功提示 "考场配置成功"

  场景: 管理员配置教室
    假如 我已登录为租户管理员
    并且 已配置考场 "第一考场"
    当 我点击"配置教室"按钮
    并且 我填写教室信息
      | 教室名称   | 101教室                 |
      | 教室编号   | ROOM-101                |
      | 容量       | 30                      |
    并且 我点击保存按钮
    那么 教室配置应该成功
    并且 我应该看到教室 "101教室" 在教室列表中

  @smoke
  场景: 管理员触发座位安排
    假如 我已登录为租户管理员
    并且 已配置考场和教室
    并且 已统计参考人数 "150人"
    当 我点击"开始安排座位"按钮
    并且 我选择安排规则 "按岗位分组"
    并且 我确认开始安排
    那么 系统应该开始座位安排
    并且 我应该看到安排进度
    并且 安排完成后应该显示成功提示

  @smoke
  场景: 系统自动分配座位
    假如 系统开始座位安排
    并且 考场有5个教室，每个教室30个座位
    并且 有150名考生需要安排座位
    当 系统执行座位分配算法
    那么 系统应该按岗位分组
    并且 相同岗位的考生应该在同一教室
    并且 每个考生应该分配唯一的座位号
    并且 座位号应该按顺序排列
    并且 所有考生都应该有座位

  场景: 管理员查看座位分配结果
    假如 我已登录为租户管理员
    并且 座位安排已完成
    当 我访问座位安排页面
    那么 我应该看到座位分配统计
      | 统计项     |
      | 总人数     |
      | 已分配     |
      | 未分配     |
      | 考场数     |
      | 教室数     |
    并且 我可以按考场查看详情
    并且 我可以按教室查看座位表

  场景: 管理员导出座位表
    假如 我已登录为租户管理员
    并且 座位安排已完成
    当 我点击"导出座位表"按钮
    并且 我选择导出格式 "Excel"
    并且 我选择导出范围 "全部考场"
    并且 我点击"确认导出"按钮
    那么 系统应该生成座位表Excel文件
    并且 我应该能够下载文件
    并且 文件应该包含所有考生的座位信息

  场景: 管理员重新安排座位
    假如 我已登录为租户管理员
    并且 座位安排已完成
    当 我点击"重新安排"按钮
    并且 系统提示将清除现有安排
    并且 我确认重新安排
    那么 系统应该清除现有座位分配
    并且 系统应该重新执行座位安排
    并且 准考证应该更新座位信息

  场景: 座位安排规则验证
    假如 我已登录为租户管理员
    并且 我在座位安排页面
    当 我配置座位安排规则
      | 规则       | 值                      |
      | 分组方式   | 按岗位分组              |
      | 间隔座位   | 是                      |
      | 随机排序   | 否                      |
    并且 我开始安排座位
    那么 系统应该按照规则执行
    并且 相同岗位考生应该在同一教室
    并且 座位之间应该有间隔
    并且 座位号应该按顺序排列

