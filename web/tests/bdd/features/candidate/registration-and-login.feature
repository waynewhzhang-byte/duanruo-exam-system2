# language: zh-CN
@p0 @candidate
功能: 考生注册与登录
  作为考生
  我想要注册账号并登录系统
  以便参加考试报名

  背景:
    假如 系统已经启动并运行

  场景: 考生成功注册
    当 我访问注册页面
    并且 我填写注册信息
      | 真实姓名   | 张三                    |
      | 用户名     | test_candidate_001      |
      | 邮箱       | zhangsan@test.com       |
      | 密码       | Test123!@#              |
      | 确认密码   | Test123!@#              |
    并且 我点击注册按钮
    那么 注册应该成功
    并且 我应该收到注册成功的提示
    并且 我应该能够使用新账号登录

  @smoke
  场景: 考生成功登录
    假如 存在考生账号 "bdd_candidate"
    当 我使用用户名 "bdd_candidate" 和密码 "Candidate123!@#" 登录
    那么 登录应该成功
    并且 我应该看到考生仪表盘
    并且 页面URL应该包含 "/candidate"

  @smoke
  场景: 使用错误密码登录失败
    假如 存在考生账号 "bdd_candidate"
    当 我使用用户名 "bdd_candidate" 和密码 "WrongPassword" 登录
    那么 登录应该失败
    并且 我应该看到错误提示 "用户名或密码错误"

  @smoke
  场景: 使用不存在的用户名登录失败
    当 我使用用户名 "nonexistent_user" 和密码 "Test123!@#" 登录
    那么 登录应该失败
    并且 我应该看到错误提示 "用户名或密码错误"

  场景: 注册时密码不一致
    当 我访问注册页面
    并且 我填写注册信息
      | 真实姓名   | 李四                    |
      | 用户名     | test_candidate_002      |
      | 邮箱       | lisi@test.com           |
      | 密码       | Test123!@#              |
      | 确认密码   | DifferentPassword       |
    并且 我点击注册按钮
    那么 注册应该失败
    并且 我应该看到错误提示 "两次输入的密码不一致"

  场景: 注册时使用已存在的用户名
    假如 存在考生账号 "bdd_candidate"
    当 我访问注册页面
    并且 我填写注册信息
      | 真实姓名   | 王五                    |
      | 用户名     | bdd_candidate           |
      | 邮箱       | wangwu@test.com         |
      | 密码       | Test123!@#              |
      | 确认密码   | Test123!@#              |
    并且 我点击注册按钮
    那么 注册应该失败
    并且 我应该看到错误提示 "用户名已存在"

