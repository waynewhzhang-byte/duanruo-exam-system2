# language: zh-CN
@p0 @reviewer @review
功能: 审核流程
  作为审核员
  我想要审核考生的报名申请
  以便筛选合格的考生

  背景:
    假如 系统已经启动并运行
    并且 存在一级审核员账号 "bdd_reviewer1"
    并且 存在二级审核员账号 "bdd_reviewer2"
    并且 已存在考生报名申请

  @smoke
  场景: 自动审核通过
    假如 考生提交的报名信息符合所有自动审核规则
    当 系统执行自动审核
    那么 报名状态应该变更为 "一级审核中"
    并且 系统应该创建一级审核任务
    并且 任务应该分配给一级审核员

  场景: 自动审核拒绝
    假如 考生提交的报名信息不符合自动审核规则
      | 规则       | 要求       | 实际值     |
      | 性别       | 男         | 女         |
    当 系统执行自动审核
    那么 报名状态应该变更为 "自动拒绝"
    并且 系统应该发送拒绝通知给考生
    并且 考生应该能够查看拒绝原因 "性别不符合要求"

  @smoke
  场景: 一级审核员查看待审核任务
    假如 我使用用户名 "bdd_reviewer1" 和密码 "Reviewer123!@#" 登录
    当 我访问"审核队列"页面
    那么 我应该看到分配给我的所有待审核任务
    并且 每个任务应该显示
      | 信息项     |
      | 考生姓名   |
      | 报考岗位   |
      | 提交时间   |
      | 审核状态   |
    并且 我可以点击查看详情

  @smoke
  场景: 一级审核员审核通过
    假如 我已登录为一级审核员
    并且 我在审核详情页面
    当 我查看考生信息和附件
    并且 我确认信息真实有效
    并且 我点击"审核通过"按钮
    并且 我输入审核意见 "材料齐全，信息真实"
    并且 我点击"提交"按钮
    那么 审核结果应该保存成功
    并且 报名状态应该变更为 "二级审核中"
    并且 系统应该创建二级审核任务
    并且 考生应该收到审核通过通知

  场景: 一级审核员审核拒绝
    假如 我已登录为一级审核员
    并且 我在审核详情页面
    当 我发现信息不符合要求
    并且 我点击"审核拒绝"按钮
    并且 我输入拒绝原因 "学历证明不清晰"
    并且 我点击"提交"按钮
    那么 审核结果应该保存成功
    并且 报名状态应该变更为 "一级拒绝"
    并且 考生应该收到审核拒绝通知
    并且 考生应该能够查看拒绝原因

  @smoke
  场景: 二级审核员查看待审核任务
    假如 我使用用户名 "bdd_reviewer2" 和密码 "Reviewer123!@#" 登录
    当 我访问"审核队列"页面
    那么 我应该看到分配给我的所有待审核任务
    并且 任务应该是通过一级审核的申请
    并且 我可以查看一级审核意见

  @smoke
  场景: 二级审核员审核通过
    假如 我已登录为二级审核员
    并且 我在审核详情页面
    当 我查看考生信息、附件和一级审核意见
    并且 我确认信息真实有效
    并且 我点击"审核通过"按钮
    并且 我输入审核意见 "最终审核通过"
    并且 我点击"提交"按钮
    那么 审核结果应该保存成功
    并且 报名状态应该变更为 "审核通过"
    并且 考生应该收到审核通过通知
    并且 考生应该可以进行缴费

  场景: 二级审核员审核拒绝
    假如 我已登录为二级审核员
    并且 我在审核详情页面
    当 我发现信息存在问题
    并且 我点击"审核拒绝"按钮
    并且 我输入拒绝原因 "附件材料存在疑点"
    并且 我点击"提交"按钮
    那么 审核结果应该保存成功
    并且 报名状态应该变更为 "二级拒绝"
    并且 考生应该收到审核拒绝通知

  场景: 审核员批量审核
    假如 我已登录为一级审核员
    并且 我在审核队列页面
    当 我选择多个待审核任务
    并且 我点击"批量审核通过"按钮
    并且 我输入批量审核意见 "批量审核通过"
    并且 我确认批量审核
    那么 所有选中的任务应该审核通过
    并且 所有任务状态应该变更为 "二级审核中"

  场景: 审核员查看审核历史
    假如 我已登录为审核员
    当 我访问"审核历史"页面
    那么 我应该看到我审核过的所有任务
    并且 每个任务应该显示
      | 信息项     |
      | 考生姓名   |
      | 审核时间   |
      | 审核结果   |
      | 审核意见   |
    并且 我可以按时间、结果筛选

  场景: 审核员导出审核报告
    假如 我已登录为审核员
    并且 我在审核历史页面
    当 我选择时间范围 "2025-11-01" 到 "2025-11-30"
    并且 我点击"导出报告"按钮
    那么 系统应该生成审核报告
    并且 报告应该包含所有审核记录
    并且 我应该能够下载Excel文件

  场景: 审核任务自动分配
    假如 系统配置了审核员自动分配规则
    并且 存在多个一级审核员
    当 考生提交报名申请并通过自动审核
    那么 系统应该自动分配审核任务
    并且 任务应该均匀分配给审核员
    并且 审核员应该收到新任务通知

