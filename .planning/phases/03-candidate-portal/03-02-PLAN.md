---
phase: 03-candidate-portal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/candidate/applications/new/page.tsx
  - web/src/lib/api-hooks.ts
autonomous: true
requirements:
  - CAND-01

must_haves:
  truths:
    - "候选人填写报名表单并上传文件后点击提交，报名成功且不报错"
  artifacts:
    - path: "web/src/app/candidate/applications/new/page.tsx"
      provides: "报名表单页面，支持文件上传和表单提交"
      contains: "useSubmitApplication"
  key_links:
    - from: "web/src/app/candidate/applications/new/page.tsx"
      to: "POST /applications"
      via: "useSubmitApplication"
      pattern: "submit.mutate"
---

<objective>
Fix the candidate application form to properly submit applications with file uploads.

The current form at `/candidate/applications/new/page.tsx`:
1. Has @ts-nocheck which should be removed
2. Has corrupted/quarantined blocks that need cleanup
3. Uses form hooks and submission properly
4. Has file upload components

Output: Working application form that can submit successfully.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Remove @ts-nocheck and fix TypeScript errors</name>
  <files>web/src/app/candidate/applications/new/page.tsx</files>
  <action>
    1. Remove `// @ts-nocheck` from line 1
    2. Run TypeScript check to see remaining errors:
    ```bash
    cd web && npx tsc --noEmit 2>&1 | grep -A2 "new/page.tsx"
    ```
    3. Fix any TypeScript errors found (type assertions, missing props, etc.)
  </action>
  <verify>
    <automated>cd /Users/Zhuanz/duanruo-exam-system2/duanruo-exam-system2/web && npx tsc --noEmit 2>&1 | grep -c "error TS"</automated>
  </verify>
  <done>
    - @ts-nocheck removed
    - TypeScript errors resolved
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean up corrupted code blocks</name>
  <files>web/src/app/candidate/applications/new/page.tsx</files>
  <action>
    Around lines 91-150, there's a quarantined block with corrupted text:
    - "/* FIX: quarantine corrupted block below"
    - Corrupted text with special characters
    
    Remove or fix this corrupted block. The auto-save draft functionality should be restored if it was working before, or properly implemented now.
  </action>
  <verify>
    <automated>grep -n "quarantine" web/src/app/candidate/applications/new/page.tsx</automated>
  </verify>
  <done>
    - Corrupted block removed or fixed
    - File is clean
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify form submission works</name>
  <files>web/src/app/candidate/applications/new/page.tsx</files>
  <action>
    Check the form submission:
    1. Verify `useSubmitApplication` hook is imported and used correctly
    2. Verify form data is properly structured for submission
    3. Ensure file uploads are handled properly (FormFileUpload component)
    
    The form should:
    - Call submit.mutate with form data on submit
    - Handle success: redirect to applications list or show success message
    - Handle error: show toast with error message
  </action>
  <verify>
    <automated>grep -n "submit.mutate" web/src/app/candidate/applications/new/page.tsx</automated>
  </verify>
  <done>
    - Form submission logic present
    - Error handling in place
  </done>
</task>

</tasks>

<verification>
- TypeScript type-check passes with 0 errors related to this file
- No corrupted code blocks in the file
- Form submission logic is intact
</verification>

<success_criteria>
Candidate can fill out the application form with file uploads and submit successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/03-candidate-portal/03-02-SUMMARY.md`
</output>
