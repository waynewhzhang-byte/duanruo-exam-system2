---
phase: 03-candidate-portal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/candidate/applications/page.tsx
  - web/src/lib/api-hooks.ts
autonomous: true
requirements:
  - CAND-02

must_haves:
  truths:
    - "我的报名列表正确显示申请状态（草稿/已提交/审核中/已通过等）和相关信息"
    - "使用 apiGetWithTenant 正确携带租户上下文"
  artifacts:
    - path: "web/src/app/candidate/applications/page.tsx"
      provides: "候选人报名列表页，正确获取并展示报名数据"
      contains: "apiGetWithTenant"
  key_links:
    - from: "web/src/app/candidate/applications/page.tsx"
      to: "GET /applications/my"
      via: "apiGetWithTenant"
      pattern: "apiGetWithTenant.*applications/my"
---

<objective>
Fix candidate applications list to properly fetch and display application data with correct tenant context.

The current page at `/candidate/applications/page.tsx` has several issues:
1. Uses `apiGet` instead of `apiGetWithTenant` for fetching tenant-scoped data
2. Needs proper status display aligned with backend statuses
3. Needs progress stepper UI

Output: Fixed applications list with proper tenant context and status display.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Fix tenant context in applications list</name>
  <files>web/src/app/candidate/applications/page.tsx</files>
  <action>
    Make these changes to web/src/app/candidate/applications/page.tsx:

    1. Import useTenant hook (if not already imported):
    ```typescript
    import { useTenant } from '@/hooks/useTenant'
    ```

    2. Add tenant hook usage:
    ```typescript
    const { tenant } = useTenant()
    ```

    3. Replace `apiGet` with `apiGetWithTenant` for fetching applications:
    - Line ~94: Change `apiGet<ApplicationListResponse>(...)` to `apiGetWithTenant<ApplicationListResponse>(..., tenant.id)`
    - Change queryKey from `['applications', 'my', selectedTenantId, ...]` to `['applications', 'my', tenant?.id, ...]`
    - Change enabled from `!!selectedTenantId` to `!!tenant?.id`

    4. Remove the tenants query and selectedTenantId state (lines 67-80) since we're now using useTenant hook directly
  </action>
  <verify>
    <automated>cd /Users/Zhuanz/duanruo-exam-system2/duanruo-exam-system2/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - apiGetWithTenant used for applications/my endpoint
    - useTenant hook used for tenant context
    - No more selectedTenantId state
    - TypeScript passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify status badge alignment with backend</name>
  <files>web/src/app/candidate/applications/page.tsx</files>
  <action>
    Check the getStatusBadge function (around line 130-143) aligns with backend statuses. Backend statuses should include:
    - DRAFT, SUBMITTED, PAID
    - PENDING_PRIMARY_REVIEW, PRIMARY_PASSED
    - PENDING_SECONDARY_REVIEW
    - APPROVED, REJECTED
    - TICKET_ISSUED, SEAT_ASSIGNED

    If any are missing, add them to the statusMap.
  </action>
  <verify>
    <automated>grep -n "getStatusBadge" web/src/app/candidate/applications/page.tsx</automated>
  </verify>
  <done>
    - All backend statuses have badge mapping
  </done>
</task>

</tasks>

<verification>
- `grep apiGetWithTenant.*applications web/src/app/candidate/applications/page.tsx` shows correct usage
- TypeScript type-check passes
- Page loads without console errors
</verification>

<success_criteria>
Candidate can view their applications list with correct status and tenant context.
</success_criteria>

<output>
After completion, create `.planning/phases/03-candidate-portal/03-01-SUMMARY.md`
</output>
