---
phase: 03-candidate-portal
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - web/src/app/[tenantSlug]/candidate/tickets/page.tsx
  - web/src/app/[tenantSlug]/candidate/applications/[id]/payment/page.tsx
autonomous: true
requirements:
  - CAND-03
  - CAND-04

must_haves:
  truths:
    - "审核通过且缴费完成的候选人可点击按钮下载准考证 PDF，下载成功"
    - "候选人进入缴费页面并完成缴费操作，全程无报错且缴费状态正确更新"
  artifacts:
    - path: "web/src/app/[tenantSlug]/candidate/tickets/page.tsx"
      provides: "准考证列表页，支持 PDF 下载"
      contains: "download"
    - path: "web/src/app/[tenantSlug]/candidate/applications/[id]/payment/page.tsx"
      provides: "缴费页面，支持缴费流程"
      contains: "useInitiatePayment"
  key_links:
    - from: "tickets/page.tsx"
      to: "GET /tickets/my"
      via: "apiGetWithTenant"
    - from: "payment/page.tsx"
      to: "POST /payments/initiate"
      via: "useInitiatePayment"
---

<objective>
Fix ticket download and payment flow for candidates.

Current state:
1. Tickets page (`/candidate/tickets/page.tsx`) - has download functionality using apiGetWithTenant
2. Payment page (`/candidate/applications/[id]/payment/page.tsx`) - has payment flow

Verify and fix any issues with these flows.

Output: Working ticket download and payment flow.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Fix ticket download functionality</name>
  <files>web/src/app/[tenantSlug]/candidate/tickets/page.tsx</files>
  <action>
    Check and fix the ticket download flow:

    1. Verify apiGetWithTenant is used for fetching tickets (line 52)
    2. Check download URL format (line 61-65) - should include tenant context
    3. Ensure download uses proper authentication
    
    The current implementation uses `/api/v1/tickets/${ticket.id}/download` directly which may not carry tenant info. Should use apiGetWithTenant or include tenant in the URL.
  </action>
  <verify>
    <parameter name="automated">grep -n "apiGetWithTenant" web/src/app/\[tenantSlug\]/candidate/tickets/page.tsx</parameter>
  </verify>
  <done>
    - Ticket fetch uses apiGetWithTenant
    - Download URL includes tenant context
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix payment flow tenant context</name>
  <files>web/src/app/[tenantSlug]/candidate/applications/[id]/payment/page.tsx</files>
  <action>
    Check and fix the payment flow:

    1. Verify useTenant hook is imported and used (line 41)
    2. Ensure API calls use tenant context
    3. Check payment initiation uses proper tenant scope
    
    Verify:
    - useApplication uses tenant context (should use apiGetWithTenant internally)
    - useInitiatePayment uses proper tenant context
  </action>
  <verify>
    <parameter name="automated">grep -n "useTenant" web/src/app/\[tenantSlug\]/candidate/applications/\[id\]/payment/page.tsx | head -5</parameter>
  </verify>
  <done>
    - useTenant hook present
    - Payment API calls use tenant context
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify payment status handling</name>
  <files>web/src/app/[tenantSlug]/candidate/applications/[id]/payment/page.tsx</files>
  <action>
    Check payment status handling:

    1. Look at handlePaymentSuccess (around line 200+) - should redirect or show success
    2. Look at handlePaymentError - should show error and offer retry
    3. Verify the flow matches user decision: manual refresh button to check status
    
    The user decided: "Manual refresh — candidate clicks button to check if payment succeeded"
    So there should be a manual refresh/check button, not auto-polling.
  </action>
  <verify>
    <parameter name="automated">grep -n "refresh\|polling" web/src/app/\[tenantSlug\]/candidate/applications/\[id\]/payment/page.tsx</parameter>
  </verify>
  <done>
    - Manual refresh button present
    - No auto-polling (per user decision)
  </done>
</task>

</tasks>

<verification>
- Ticket download uses proper tenant context
- Payment flow uses tenant context
- Manual refresh for payment status (no auto-polling)
</verification>

<success_criteria>
Candidate can download ticket PDF and complete payment flow with proper status updates.
</success_criteria>

<output>
After completion, create `.planning/phases/03-candidate-portal/03-03-SUMMARY.md`
</output>
