---
phase: 02-admin-backend
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/[tenantSlug]/admin/exams/page.tsx
autonomous: true
gap_closure: true
requirements:
  - ADMIN-02

must_haves:
  truths:
    - "考试列表页的考试时间列显示实际考试开始/结束时间，而非'未设置'"
  artifacts:
    - path: "web/src/app/[tenantSlug]/admin/exams/page.tsx"
      provides: "考试列表页 Exam 接口使用 examStart/examEnd，JSX 渲染这两个字段"
      contains: "examStart?: string"
  key_links:
    - from: "web/src/app/[tenantSlug]/admin/exams/page.tsx"
      to: "backend GET /exams response"
      via: "Exam interface field names"
      pattern: "examStart.*examEnd"
---

<objective>
Close the single remaining gap in ADMIN-02: the exam list page (`exams/page.tsx`) has a residual field name mismatch in its local `Exam` interface. The field `examDate?: string` at line 36 is never populated by the backend (which returns `examStart`/`examEnd`), causing the exam time column to always display "未设置" for every exam.

Purpose: The exam time column in the admin exam list should show real exam time data when exam times are set.
Output: `exams/page.tsx` with corrected `Exam` interface and JSX render.
</objective>

<execution_context>
@/Users/Zhuanz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Zhuanz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Context from verification:
- All other ADMIN-01 through ADMIN-07 requirements are verified and complete.
- 02-01-PLAN.md fixed ExamsResponse pagination type and the detail/edit page Exam interfaces, but left the list page Exam interface with the old `examDate` field.
- The detail page (`detail/page.tsx` lines 34-37) and edit page already use `examStart`/`examEnd` correctly — this fix should mirror that pattern.
- Backend `mapToResponse` confirmed to return `examStart`/`examEnd` (exam.service.ts lines 182-183).

<interfaces>
<!-- From web/src/app/[tenantSlug]/admin/exams/page.tsx (current state — DO NOT keep) -->
<!-- Line 36 (broken): examDate?: string -->
<!-- Line 394 (broken): {exam.examDate || '未设置'} -->

<!-- From web/src/app/[tenantSlug]/admin/exams/[examId]/detail/page.tsx (correct pattern to mirror) -->
interface Exam {
  ...
  examStart?: string
  examEnd?: string
  ...
}
<!-- JSX pattern in detail page: {exam.examStart} ~ {exam.examEnd} -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Exam interface and JSX in exams/page.tsx</name>
  <files>web/src/app/[tenantSlug]/admin/exams/page.tsx</files>
  <action>
    Make exactly two changes to this file:

    1. In the `Exam` interface (around line 36), replace:
       ```
       examDate?: string
       ```
       with:
       ```
       examStart?: string
       examEnd?: string
       ```

    2. In the JSX table cell (around line 394), replace:
       ```
       <div>考试: {exam.examDate || '未设置'}</div>
       ```
       with:
       ```
       <div>考试: {exam.examStart || '未设置'} ~ {exam.examEnd || '未设置'}</div>
       ```

    Do NOT change anything else in the file. The ExamsResponse type (lines 42-48) is already correct (pageSize/currentPage/totalElements). The registration time row at line 393 is already correct.
  </action>
  <verify>
    <automated>cd /Users/Zhuanz/duanruo-exam-system2/duanruo-exam-system2/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - `Exam` interface contains `examStart?: string` and `examEnd?: string` (no `examDate`)
    - JSX at the exam time row renders `exam.examStart` and `exam.examEnd`
    - `npx tsc --noEmit` exits with 0 errors related to this file
  </done>
</task>

</tasks>

<verification>
After the fix:
- `grep -n "examDate" web/src/app/[tenantSlug]/admin/exams/page.tsx` returns no matches
- `grep -n "examStart" web/src/app/[tenantSlug]/admin/exams/page.tsx` shows the interface field and JSX usage
- TypeScript type-check passes with 0 new errors
</verification>

<success_criteria>
The exam list page `Exam` interface no longer contains `examDate`. Both `examStart` and `examEnd` are declared in the interface and rendered in the table cell. When an exam has exam times set in the backend, they will appear in the list view rather than showing "未设置".
</success_criteria>

<output>
After completion, create `.planning/phases/02-admin-backend/02-05-SUMMARY.md` with standard summary format.
</output>
