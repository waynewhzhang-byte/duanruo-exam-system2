---
phase: 04-reviewer-workflow
plan: "03"
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - web/src/app/[tenantSlug]/reviewer/applications/[id]/page.tsx
autonomous: true
requirements: [REVW-02, REVW-03]
user_setup: []
must_haves:
  truths:
    - Approve button sends correct API request and shows success/error
    - Reject button requires reason and sends correct API request
    - Detail page shows complete candidate information
    - Detail page shows form payload and attachments
  artifacts:
    - path: web/src/app/[tenantSlug]/reviewer/applications/[id]/page.tsx
      provides: Application detail with review actions (approve/reject/return)
      uses_apis:
        - GET /applications/{id}
        - POST /reviews/{id}/approve
        - POST /reviews/{id}/reject
  key_links:
    - from: web/src/app/[tenantSlug]/reviewer/applications/[id]/page.tsx
      to: /applications/{id}
      via: apiGetWithTenant
    - from: web/src/app/[tenantSlug]/reviewer/applications/[id]/page.tsx
      to: /reviews/{id}/approve|reject
      via: apiPostWithTenant
---

<objective>
Verify review actions (approve/reject/return) work correctly and detail page shows complete candidate information.

Purpose: Ensure the detail page displays all candidate data and review actions properly update application status.

Output: Working review detail page with functional approve/reject buttons.
</objective>

<execution_context>
@/Users/Zhuanz/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-reviewer-workflow/04-CONTEXT.md

# Review Actions

## Approve
- API: POST /reviews/{applicationId}/approve
- Body: { comment?: string }
- Success: toast.success('审核通过'), navigate to queue
- Error: toast.error(err?.message)

## Reject  
- API: POST /reviews/{applicationId}/reject
- Body: { comment: string (required) }
- Success: toast.success('已拒绝该申请'), navigate to queue
- Error: toast.error(err?.message)

# Detail Page Display Sections

1. **Header**: Exam title, position title, status badge
2. **Candidate Basic Info**: name, idCardNumber, phone, email
3. **Form Payload**: Dynamic fields from application payload
4. **Attachments**: List of uploaded files with preview
5. **Auto Check Result**: If present
6. **Review Progress**: Statistics panel
7. **Review Actions**: Approve/Reject buttons with comments
</context>

<tasks>

<task type="auto">
  <name>Verify application detail loads correctly</name>
  <files>web/src/app/[tenantSlug]/reviewer/applications/[id]/page.tsx</files>
  <action>
    Review the detail page at lines 115-122:
    - Uses useQuery with key ['application-detail', applicationId, tenant?.id]
    - Calls apiGetWithTenant('/applications/${applicationId}')
    - Returns ApplicationDetailResponse
    
    Verify:
    1. Response type matches actual API response
    2. Error handling for failed loads
    3. Loading state shown
    
    Also verify the form template fetch (lines 125-132) for field labels.
  </action>
  <verify>Code review: Check API call, response type, error handling</verify>
  <done>Application detail loads with all required data</done>
</task>

<task type="auto">
  <name>Verify candidate information display</name>
  <files>web/src/app/[tenantSlug]/reviewer/applications/[id]/page.tsx</files>
  <action>
    Review candidate info display (lines 335-360):
    - candidateName (line 344)
    - candidateIdCardNumber (line 348)
    - candidatePhone (line 352)
    - candidateEmail (line 356)
    
    Verify these fields exist in the API response type (ApplicationDetailResponse lines 21-51).
    
    Also verify the form payload display (lines 386-472):
    - Iterates over application.payload
    - Gets field label from form template or default labels
    - Shows display value or falls back to candidate info
  </action>
  <verify>Code review: Verify all displayed fields exist in response type</verify>
  <done>Candidate info and form payload display correctly</done>
</task>

<task type="auto">
  <name>Verify attachment display and preview</name>
  <files>web/src/app/[tenantSlug]/reviewer/applications/[id]/page.tsx</files>
  <action>
    Review attachment handling:
    - Attachment display: lines 481-605
    - Preview function: lines 185-193
    
    Verify:
    1. Attachments list from application.attachments
    2. Each shows fileName, contentType, fileSize
    3. Preview button calls /files/{fileId}/download-url
    4. Preview opens in modal (lines 787-797)
  </action>
  <verify>Code review: Check attachment list, preview URL fetch, modal display</verify>
  <done>Attachments display with working preview</done>
</task>

<task type="auto">
  <name>Verify approve action</name>
  <files>web/src/app/[tenantSlug]/reviewer/applications/[id]/page.tsx</files>
  <action>
    Review approve flow:
    - Lines 135-151: approveMutation using apiPostWithTenant
    - Endpoint: /reviews/${applicationId}/approve
    - Body: { comment: reviewComments || '审核通过' }
    - Lines 172-174: handleApprove calls mutation
    - Lines 712-719: Button with disabled check
    
    Verify:
    1. API call uses correct endpoint and method
    2. Request body is correct
    3. Success shows toast and navigates to queue
    4. Error shows error toast
    5. Button disabled when pending or has rejected/returned items
  </action>
  <verify>Code review: Check API endpoint, request body, success/error handling</verify>
  <done>Approve action works correctly</done>
</task>

<task type="auto">
  <name>Verify reject action</name>
  <files>web/src/app/[tenantSlug]/reviewer/applications/[id]/page.tsx</files>
  <action>
    Review reject flow:
    - Lines 154-170: rejectMutation using apiPostWithTenant
    - Endpoint: /reviews/${applicationId}/reject  
    - Body: { comment: rejectReason }
    - Lines 176-182: handleReject validates reason required
    - Lines 772-780: Button with validation
    
    Verify:
    1. API call uses correct endpoint
    2. Requires rejectReason (not empty)
    3. Shows error if reason missing
    4. Success/error handling correct
  </action>
  <verify>Code review: Check API endpoint, validation, error handling</verify>
  <done>Reject action works correctly</done>
</task>

</tasks>

<verification>
1. Code review: All API endpoints and methods correct
2. Code review: Request/response handling correct
3. Code review: Error states handled with user feedback
4. No TypeScript errors
</verification>

<success_criteria>
- Application detail page shows complete candidate information
- Form payload and attachments display correctly
- Approve button works and shows success feedback
- Reject button requires reason and works correctly
- After action, navigates back to queue with updated status
</success_criteria>

<output>
After completion, create `.planning/phases/04-reviewer-workflow/04-03-SUMMARY.md`
</output>
