---
phase: 04-reviewer-workflow
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/reviewer/applications/page.tsx
  - web/src/app/[tenantSlug]/reviewer/queue/page.tsx
  - web/src/app/[tenantSlug]/reviewer/page.tsx
autonomous: true
requirements: [REVW-01, REVW-02, REVW-03]
user_setup: []
must_haves:
  truths:
    - Reviewer pages use tenant-scoped routes (/[tenantSlug]/reviewer/*)
    - All API calls use apiGetWithTenant/apiPostWithTenant for X-Tenant-ID header
    - Deprecated non-tenant pages are removed or redirected
  artifacts:
    - path: web/src/app/[tenantSlug]/reviewer/queue/page.tsx
      provides: Review queue with pending applications
      min_lines: 280
    - path: web/src/app/[tenantSlug]/reviewer/applications/[id]/page.tsx
      provides: Application detail with review actions
      min_lines: 800
  key_links:
    - from: web/src/app/[tenantSlug]/reviewer/queue/page.tsx
      to: /applications/pending-review
      via: apiGetWithTenant
      pattern: apiGetWithTenant.*pending-review
---

<objective>
Fix reviewer pages to use proper tenant context and remove duplicate implementations.

Purpose: Ensure all reviewer pages correctly use tenant context (X-Tenant-ID header) for API calls.

Output: Cleaned up reviewer pages using tenant-scoped routes with proper API calls.
</objective>

<execution_context>
@/Users/Zhuanz/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-reviewer-workflow/04-CONTEXT.md

# Key Finding
The page at `/app/reviewer/applications/page.tsx` (non-tenant-scoped) uses `apiGet` without tenant context, while the correct version at `/app/[tenantSlug]/reviewer/queue/page.tsx` uses `apiGetWithTenant`.

# Solution
Remove the non-tenant-scoped reviewer pages and ensure tenant-scoped versions are working correctly.
</context>

<tasks>

<task type="auto">
  <name>Remove deprecated non-tenant reviewer pages</name>
  <files>web/src/app/reviewer/applications/page.tsx</files>
  <action>
    Delete the non-tenant-scoped reviewer pages that use apiGet without tenant context:
    - web/src/app/reviewer/page.tsx
    - web/src/app/reviewer/layout.tsx
    - web/src/app/reviewer/review/[applicationId]/page.tsx
    - web/src/app/reviewer/tasks/page.tsx
    - web/src/app/reviewer/queue/page_old.tsx
    - web/src/app/reviewer/queue/page.tsx (non-tenant version)
    - web/src/app/reviewer/history/page.tsx (non-tenant version)
    - web/src/app/reviewer/applications/page.tsx
    
    Keep only the tenant-scoped versions under /app/[tenantSlug]/reviewer/*
  </action>
  <verify>ls -la web/src/app/reviewer/ 2>/dev/null || echo "Directory removed or empty"</verify>
  <done>Non-tenant-scoped /reviewer/* pages removed, only /[tenantSlug]/reviewer/* remains</done>
</task>

<task type="auto">
  <name>Verify tenant-scoped reviewer pages exist</name>
  <files>web/src/app/[tenantSlug]/reviewer/queue/page.tsx</files>
  <action>
    Verify these tenant-scoped reviewer pages exist and use apiGetWithTenant:
    - /[tenantSlug]/reviewer/page.tsx (main reviewer dashboard)
    - /[tenantSlug]/reviewer/queue/page.tsx (pending applications)
    - /[tenantSlug]/reviewer/history/page.tsx (review history)
    - /[tenantSlug]/reviewer/applications/[id]/page.tsx (application detail)
    
    Confirm each uses apiGetWithTenant or apiPostWithTenant for API calls.
  </action>
  <verify>grep -l "apiGetWithTenant\|apiPostWithTenant" web/src/app/\[tenantSlug\]/reviewer/**/*.tsx</verify>
  <done>All tenant-scoped reviewer pages use tenant context for API calls</done>
</task>

</tasks>

<verification>
1. Check that no files exist at web/src/app/reviewer/*.tsx (non-tenant)
2. Check that web/src/app/[tenantSlug]/reviewer/queue/page.tsx uses apiGetWithTenant
3. Check that web/src/app/[tenantSlug]/reviewer/applications/[id]/page.tsx uses apiGetWithTenant
</verification>

<success_criteria>
- Deprecated /reviewer/* pages removed
- Tenant-scoped /[tenantSlug]/reviewer/* pages verified to use apiGetWithTenant
- No runtime errors due to missing tenant context
</success_criteria>

<output>
After completion, create `.planning/phases/04-reviewer-workflow/04-01-SUMMARY.md`
</output>
