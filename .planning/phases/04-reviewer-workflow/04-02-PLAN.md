---
phase: 04-reviewer-workflow
plan: "02"
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - web/src/app/[tenantSlug]/reviewer/queue/page.tsx
autonomous: true
requirements: [REVW-01]
user_setup: []
must_haves:
  truths:
    - Review queue page loads pending applications from backend
    - Applications correctly filtered by reviewer role (primary/secondary)
    - Queue displays candidate name, position, exam, status correctly
  artifacts:
    - path: web/src/app/[tenantSlug]/reviewer/queue/page.tsx
      provides: Pending applications list with pagination
      uses_api: /applications/pending-review
  key_links:
    - from: web/src/app/[tenantSlug]/reviewer/queue/page.tsx
      to: /applications/pending-review
      via: apiGetWithTenant
---

<objective>
Verify review queue correctly loads and displays pending applications.

Purpose: Ensure the queue page loads pending review applications and displays them correctly with proper data fields.

Output: Working review queue that shows pending applications with correct information.
</objective>

<execution_context>
@/Users/Zhuanz/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-reviewer-workflow/04-CONTEXT.md

# API Endpoint
The queue page calls GET /applications/pending-review with query params:
- page: page number
- size: page size

# Expected Response Format
{
  content: ApplicationItem[],
  totalElements: number,
  totalPages: number,
  currentPage: number,
  pageSize: number,
  hasNext: boolean,
  hasPrevious: boolean
}

# ApplicationItem Fields
{
  id: string,
  applicationNumber?: string,
  examId: string,
  examTitle?: string,
  positionId: string,
  positionTitle?: string,
  candidateId: string,
  candidateName?: string,
  candidateEmail?: string,
  candidatePhone?: string,
  formVersion: number,
  status: string,
  submittedAt?: string
}
</context>

<tasks>

<task type="auto">
  <name>Verify review queue API response handling</name>
  <files>web/src/app/[tenantSlug]/reviewer/queue/page.tsx</files>
  <action>
    Review the queue page code and verify:
    1. The API response is properly typed (ApplicationPageResponse)
    2. Error handling is in place for failed API calls
    3. Loading state is shown while fetching
    4. Empty state is shown when no applications
    5. The page extracts applications from data.content correctly

    The current implementation at line 70-81 shows:
    - Uses useQuery with queryKey ['pending-review-applications', tenant?.id]
    - Calls apiGetWithTenant('/applications/pending-review?page=0&size=50')
    - Extracts applications from data?.content

    Verify this is correct and fix any issues.
  </action>
  <verify>Review the code and check: 1) Response type matches API, 2) Error/loading states handled, 3) data.content extraction correct</verify>
  <done>Queue page correctly handles API response and displays applications</done>
</task>

<task type="auto">
  <name>Verify reviewer role filtering</name>
  <files>web/src/app/[tenantSlug]/reviewer/queue/page.tsx</files>
  <action>
    Review how the queue determines reviewer role:
    - Line 66-67: Checks user?.roles?.includes('PRIMARY_REVIEWER')
    - Sets reviewLevel to 'PRIMARY' or 'SECONDARY'
    
    Verify that:
    1. The page correctly identifies primary vs secondary reviewer
    2. The API call includes role information or backend filters by role
    3. Queue shows appropriate applications based on role
    
    Note: Backend should filter applications based on the reviewer's assigned role.
  </action>
  <verify>Code review: Check if reviewLevel is used in API call or if backend handles role filtering</verify>
  <done>Queue correctly filters applications by reviewer role</done>
</task>

<task type="auto">
  <name>Verify list display fields</name>
  <files>web/src/app/[tenantSlug]/reviewer/queue/page.tsx</files>
  <action>
    Verify the queue list displays these fields correctly:
    - candidateName or candidateEmail (line 233)
    - positionTitle (line 236)
    - examTitle (line 240)
    - applicationNumber (line 239)
    - submittedAt (line 252)
    - status with label (line 245)
    
    Check for any field name mismatches between API response and UI display.
  </action>
  <verify>Code review: Verify each displayed field matches API response type</verify>
  <done>Queue displays all required fields from API response</done>
</task>

</tasks>

<verification>
1. Code review: API call uses correct endpoint with tenant context
2. Code review: Response type matches expected format
3. Code review: All displayed fields exist in API response
4. No TypeScript errors when running build
</verification>

<success_criteria>
- Review queue loads pending applications without errors
- Applications displayed with correct information (name, position, exam, status)
- Error and loading states handled properly
</success_criteria>

<output>
After completion, create `.planning/phases/04-reviewer-workflow/04-02-SUMMARY.md`
</output>
