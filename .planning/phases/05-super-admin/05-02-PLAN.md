---
phase: 05-super-admin
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/super-admin/users/page.tsx
autonomous: true
requirements: [SADM-03]
user_setup: []
must_haves:
  truths:
    - Platform users list loads from /super-admin/users API
    - Create user form works for different roles
    - User roles can be assigned (SUPER_ADMIN, TENANT_ADMIN, etc.)
    - User can be updated and deleted
  artifacts:
    - path: web/src/app/super-admin/users/page.tsx
      provides: Platform user management (list, create, update, delete)
      uses_api: /super-admin/users
---

<objective>
Verify super admin platform user management works correctly.

Purpose: Ensure platform users can be listed, created, updated, and deleted.

Output: Working user management for super admin.
</objective>

<execution_context>
@/Users/Zhuanz/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-super-admin/05-CONTEXT.md

# API Endpoints
- GET /super-admin/users?page=0&size=50 - List users
- POST /super-admin/users - Create user
- PUT /super-admin/users/{id} - Update user
- DELETE /super-admin/users/{id} - Delete user

# User Roles
- SUPER_ADMIN: System admin
- TENANT_ADMIN: Tenant admin
- PRIMARY_REVIEWER: Primary reviewer
- SECONDARY_REVIEWER: Secondary reviewer

# Notes
- Uses apiGet/apiPost without tenant context (platform-level)
- Roles config defined at lines 66-144
</context>

<tasks>

<task type="auto">
  <name>Verify platform users list loads</name>
  <files>web/src/app/super-admin/users/page.tsx</files>
  <action>
    Review user list implementation:
    - Line 159: apiGet('/super-admin/users?page=0&size=50')
    - Handles pagination and filtering
    
    Verify:
    1. API call correct
    2. Pagination handled
    3. Loading state
    4. Error handling
  </action>
  <verify>Code review: Check API call and response handling</verify>
  <done>User list loads and displays correctly</done>
</task>

<task type="auto">
  <name>Verify create user works</name>
  <files>web/src/app/super-admin/users/page.tsx</files>
  <action>
    Review user creation:
    - Lines 69-79: POST to /super-admin/users
    - Role selection: SUPER_ADMIN, TENANT_ADMIN, PRIMARY_REVIEWER, SECONDARY_REVIEWER
    - Fields: username, email, password, fullName, phoneNumber
    
    Verify:
    1. API endpoint correct
    2. Role selection works
    3. Validation present
    4. Success feedback
  </action>
  <verify>Code review: Check create user API and handling</verify>
  <done>Create user form works</done>
</task>

<task type="auto">
  <name>Verify update user works</name>
  <files>web/src/app/super-admin/users/page.tsx</files>
  <action>
    Review user update:
    - Line 99: PUT /super-admin/users/{id}
    - Can update user details
    
    Verify:
    1. API endpoint correct
    2. Update form populated with existing data
    3. Success feedback
  </action>
  <verify>Code review: Check update user API call</verify>
  <done>User can be updated</done>
</task>

<task type="auto">
  <name>Verify delete user works</name>
  <files>web/src/app/super-admin/users/page.tsx</files>
  <action>
    Review user delete:
    - Line 109: DELETE /super-admin/users/{id}
    
    Verify:
    1. API endpoint correct
    2. Confirmation dialog
    3. Success feedback
  </action>
  <verify>Code review: Check delete user API call</verify>
  <done>User can be deleted</done>
</task>

</tasks>

<verification>
1. Code review: All API endpoints correct
2. Code review: Role handling correct
3. No TypeScript errors
</verification>

<success_criteria>
- User list loads from API
- Create user with role assignment works
- Update user works
- Delete user works
</success_criteria>

<output>
After completion, create `.planning/phases/05-super-admin/05-02-SUMMARY.md`
</output>
