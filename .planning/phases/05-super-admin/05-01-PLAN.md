---
phase: 05-super-admin
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/super-admin/tenants/page.tsx
autonomous: true
requirements: [SADM-01, SADM-02]
user_setup: []
must_haves:
  truths:
    - Tenant list loads from /super-admin/tenants API
    - Create tenant form submits successfully
    - Tenant status (active/inactive) can be toggled
    - Tenant can be deleted
  artifacts:
    - path: web/src/app/super-admin/tenants/page.tsx
      provides: Tenant management (list, create, activate, deactivate, delete)
      uses_api: /super-admin/tenants
---

<objective>
Verify super admin tenant management pages work correctly.

Purpose: Ensure the super admin tenant list loads, create tenant works, and tenant operations (activate/deactivate/delete) function properly.

Output: Working tenant management for super admin.
</objective>

<execution_context>
@/Users/Zhuanz/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-super-admin/05-CONTEXT.md

# API Endpoints
- GET /super-admin/tenants?page=0&size=100 - List tenants
- POST /super-admin/tenants - Create tenant
- POST /super-admin/tenants/{id}/activate - Activate
- POST /super-admin/tenants/{id}/deactivate - Deactivate
- DELETE /super-admin/tenants/{id} - Delete

# Notes
- Uses apiGet/apiPost/apiDelete without tenant context (platform-level)
- Tenant status: ACTIVE, INACTIVE, PENDING, DELETED
</context>

<tasks>

<task type="auto">
  <name>Verify tenant list loads correctly</name>
  <files>web/src/app/super-admin/tenants/page.tsx</files>
  <action>
    Review the tenant list implementation:
    - Line 87: Calls apiGet('/super-admin/tenants?page=0&size=100')
    - Line 91-97: Handles response (checks result.content or array)
    - Loads into tenants state
    
    Verify:
    1. API call structure correct
    2. Response handling handles both paginated and array formats
    3. Loading state shown
    4. Error handling with toast
  </action>
  <verify>Code review: Check API call, response handling, loading/error states</verify>
  <done>Tenant list loads and displays correctly</done>
</task>

<task type="auto">
  <name>Verify tenant create form works</name>
  <files>web/src/app/super-admin/tenants/page.tsx</files>
  <action>
    Review tenant creation:
    - Line 110-119: Creates tenant with apiPost('/super-admin/tenants', formData)
    - Lines 107-128: formData includes name, slug, contactName, contactPhone, contactEmail, description
    - Success shows toast and refetches list
    
    Verify:
    1. API endpoint correct
    2. Required fields validated
    3. Success feedback shown
    4. List refreshes after create
  </action>
  <verify>Code review: Check API call, validation, success handling</verify>
  <done>Create tenant form submits successfully</done>
</task>

<task type="auto">
  <name>Verify tenant activate/deactivate works</name>
  <files>web/src/app/super-admin/tenants/page.tsx</files>
  <action>
    Review tenant status toggle:
    - Line 122: Activate - apiPost(`/super-admin/tenants/${tenant.id}/activate`, {})
    - Line 139: Deactivate - apiPost(`/super-admin/tenants/${selectedTenant.id}/deactivate`, {})
    
    Verify:
    1. Correct API endpoints
    2. Confirmation dialogs present
    3. Success/error handling
    4. List refreshes after action
  </action>
  <verify>Code review: Check API calls for activate/deactivate</verify>
  <done>Tenant status can be toggled</done>
</task>

<task type="auto">
  <name>Verify tenant delete works</name>
  <files>web/src/app/super-admin/tenants/page.tsx</files>
  <action>
    Review tenant delete:
    - Line 165: apiDelete(`/super-admin/tenants/${selectedTenant.id}`)
    
    Verify:
    1. Confirmation dialog present
    2. API endpoint correct
    3. Success/error handling
    4. List refreshes after delete
  </action>
  <verify>Code review: Check delete API call and handling</verify>
  <done>Tenant can be deleted</done>
</task>

</tasks>

<verification>
1. Code review: All API endpoints correct
2. Code review: Response handling handles edge cases
3. No TypeScript errors
</verification>

<success_criteria>
- Tenant list loads from API
- Create tenant form works
- Activate/deactivate toggles work
- Delete removes tenant
</success_criteria>

<output>
After completion, create `.planning/phases/05-super-admin/05-01-SUMMARY.md`
</output>
